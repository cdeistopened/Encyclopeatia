{
  "name": "ray-peat-radio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build:full",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=20.9.0",
    "npm": ">=8.0.0"
  }
}